# ── Core Data & Storage ───────────────────────────────────────────────────────
duckdb==1.5.2
pandas==3.0.2
numpy==2.0.2

# ── Data Acquisition ──────────────────────────────────────────────────────────
git+https://github.com/rongardF/tvdatafeed.git   # TradingView OHLCV (NVDA, BRN1!, EGX70)
yfinance==1.3.0                                   # Benchmark indices (SPY, VUG, XLE)
requests==2.33.1                                  # Gemma API calls
python-dotenv==1.2.2                              # API key management

# ── Data Cleaning ─────────────────────────────────────────────────────────────
beautifulsoup4==4.14.3                            # HTML stripping from news content
lxml==6.1.0                                       # HTML parser backend

# ── Time & Calendar ───────────────────────────────────────────────────────────
pandas-market-calendars==5.3.2                    # Exchange calendar (NYSE, EGX)
exchange_calendars==4.13.2                        # Precise session boundary mapping

# ── NLP / Sentiment ───────────────────────────────────────────────────────────
transformers==5.7.0                               # FinBERT (ProsusAI/finbert)
torch==2.11.0                                     # PyTorch backend for FinBERT & LSTM

# ── Machine Learning ──────────────────────────────────────────────────────────
scikit-learn==1.8.0                               # RandomForest, StandardScaler, metrics
xgboost==3.2.0                                    # Gradient boosted trees
shap==0.51.0                                      # SHAP model interpretability
slicer==0.0.8                                     # Required shap dependency pin

# ── Econometrics ──────────────────────────────────────────────────────────────
statsmodels==0.14.6                               # OLS (CAPM/FF), ARIMA
arch==8.0.0                                       # GARCH, EGARCH, TGARCH, GARCH-X
prophet==1.3.0                                    # Facebook Prophet (trend decomposition)

# ── Visualization ─────────────────────────────────────────────────────────────
matplotlib==3.10.9
seaborn==0.13.2
streamlit==1.57.0
plotly==6.7.0
st-annotated-text==4.0.2

# ── Utilities ─────────────────────────────────────────────────────────────────
tabulate==0.10.0                                  # Table formatting
scipy==1.17.1                                     # Scientific computing
